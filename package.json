{
  "name": "@fluffylabs/jam-search",
  "workspaces": [
    "backend",
    "client"
  ],
  "engines": {
    "node": "22.x",
    "npm": ">=10"
  },
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "biome": "biome ci",
    "biome-fix": "biome format --write && biome check --write && biome lint --write",
    "build": "npm run --workspaces build",
    "qa": "npm run --workspaces --if-present qa && npm run biome",
    "qa-fix": "npm run --workspaces --if-present qa-fix && npm run biome-fix && npm run qa",
    "test": "npm run --workspaces test",
    "typecheck": "npm run --workspaces typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "baseline-browser-mapping": "^2.9.15",
    "typescript": "^6.0.2"
  }
}
