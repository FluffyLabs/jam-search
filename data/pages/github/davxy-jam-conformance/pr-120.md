---
type: page
url: 'https://github.com/davxy/jam-conformance/pull/120'
title: Fuzz workflow script refactory
site: github.com/davxy/jam-conformance
created_at: '2025-11-20T12:33:24.000Z'
last_modified: '2025-11-20T12:33:24.000Z'
---

# Fuzz workflow script refactory

## Pull Request by @davxy

Parallel execution support

cc @alxmirap 


## Comment by @davxy

@alxmirap I need this merged into `main`, but your review is still valuable. We can update the script in a separate PR based on your suggestions.



## Comment by @alxmirap

I thought until now the targets created this socket and it was kind of hard-coded. If that is the case, will we rename that socket before starting the fuzzer? 


## Comment by @alxmirap

Can we have a list of targets if we don't have this option set? What happens then, are the targets launched sequently when the previous session finishes?


## Comment by @alxmirap

This is a 128-char hex string, if I'm not mistaken. Do we need a seed that big?


## Comment by @alxmirap

Iiuc, this re-invokes the python script but now with a guard to prevent it from going into recursive mode.
I think it's a clever solution, although I am a bit worried that it will be harder to reason about and modify in the future.


## Comment by @alxmirap

I would recommend adding a comment here to make clear that we are invoking the whole script again, with the safeguard that now it will always run a single target and avoid recursive execution (if I have read it correctly!)
Also, still not clear to me how the target knows what socket to communicate on.


## Comment by @davxy

Each target has an option for the socket name. See [here](https://github.com/davxy/jam-conformance/blob/main/scripts/targets.json)


## Comment by @davxy

quick and dirty :-D 
If someone feels inspired in the future it can be improved.


## Comment by @davxy

The socket name is derived from the session_id [here](https://github.com/davxy/jam-conformance/blob/738b3122cf7d9bed9821f3eb479734eb19ac481a/scripts/fuzz-workflow.py#L69)

Each recursive call gets a different session_id [here](https://github.com/davxy/jam-conformance/blob/738b3122cf7d9bed9821f3eb479734eb19ac481a/scripts/fuzz-workflow.py#L906)

The socket is passed to the target [here](https://github.com/davxy/jam-conformance/blob/738b3122cf7d9bed9821f3eb479734eb19ac481a/scripts/fuzz-workflow.py#L435). I.e. You can set the  target socket via env var 
