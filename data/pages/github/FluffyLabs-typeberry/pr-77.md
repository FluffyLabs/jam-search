---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/77'
title: 'PVM: add build commands'
site: github.com/FluffyLabs/typeberry
created_at: '2024-08-07T11:45:17.000Z'
last_modified: '2024-08-07T11:45:17.000Z'
content_kind: pr
---

# PVM: add build commands

## Pull Request by @mateuszsikora

# What?
I added build commands to generate obfuscated pvm code. 

# How to use it?

it should be enough to run 
```bash
npm run obfuscate-pvm
```
and it should generate 2 dirs:
- `pvm-dist` that contains js files 
- `pvm-dist-obfuscate` that contains the same files what `pvm-dist` but `obfuscated`


## Comment by @tomusdrw

can we do `./dist/pvm` and `./dist/pvm-obfuscated`?

That would make these two lines redundant and in future we might have more artifacts that would also end up in `dist` instead of multiplying top-level folders.


## Comment by @tomusdrw

```suggestion
    "build-pvm": "tsc --lib es2021,dom --module esnext --target es2021 --strict --esModuleInterop --skipLibCheck --forceConsistentCasingInFileNames --moduleResolution node --outDir ./dist/pvm ./packages/pvm/pvm.ts",
```


## Comment by @tomusdrw

```suggestion
    "obfuscate-pvm": "npx javascript-obfuscator ./dist/pvm --output ./dist/pvm-obfuscated --compact true --control-flow-flattening true"
```


## Comment by @mateuszsikora

sure


## Comment by @mateuszsikora

done
