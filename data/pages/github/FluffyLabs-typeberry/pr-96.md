---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/96'
title: 'PVM: adjust MemoryBuilder api to simlify useage in tests'
site: github.com/FluffyLabs/typeberry
created_at: '2024-09-05T10:39:46.000Z'
last_modified: '2024-09-05T10:39:46.000Z'
content_kind: pr
---

# PVM: adjust MemoryBuilder api to simlify useage in tests

## Pull Request by @mateuszsikora

# What?
As today [this puke](https://github.com/FluffyLabs/pvm-debugger/pull/97/commits/928c0ac50dc7a600dbc34f4d614dffa5e989158a) left our repo I decided to adjust a bit `MemoryBuilder` API to simplify parsing tests data. It's just an idea, we don't have to merge it.

pros:
- the usage is much simpler 
- the case with a few chunks on one page is covered
- I found off-by-one error :)  

cons:
- the API is not as simple as it was 


## Comment by @tomusdrw

Here's actually another off-by-one error :)
```suggestion
    check(data.length <= PAGE_SIZE, "chunk cannot be longer than one page");
```


## Comment by @tomusdrw

```suggestion
    check(data.length <= PAGE_SIZE, "chunk cannot be longer than one page");
```


## Comment by @tomusdrw

I think it should work equally well for `WritablePage` as well, right? Not sure if it makes sense to only limit that to a `VirtualPage`.


## Comment by @tomusdrw

This method doesn't seem to be necessary, I think we can easily use `store`, because it has better checks (for instance here we don't check that `data` fits into a single chunk) and handles multi-chunk writes.


## Comment by @tomusdrw

```suggestion
      page.store(startPageIndex, data);
```

I think `fill` method is superfluous, since `store` handles the same use case and does it better :)


## Comment by @tomusdrw

```suggestion
    check(getPageNumber(start) === getPageNumber(start + data.length), "The data has to fit into a single page.");
    const pageNumber = getPageNumber(start);
```


## Comment by @mateuszsikora

for now it is not needed in `ReadablePage` and `WriteablePage` classes so I decided to skip it. There is one more limitation - `ReadablePage` has `store` method implementation that returns a page fault so I would have to add a separate function. So I would limit it to VirtualPage for now and extend it when we need it.


## Comment by @mateuszsikora

done
