---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/293'
title: Service classes with detailed descriptions and updated references.
site: github.com/FluffyLabs/typeberry
created_at: '2025-03-14T10:27:14.000Z'
last_modified: '2025-03-14T10:27:14.000Z'
content_kind: pr
---

# Service classes with detailed descriptions and updated references.

## Pull Request by @DrEverr

 - updated service.ts GP links
 - added GP field names


## Comment by @DrEverr

Closing due to now providing, a lot of value.


## Comment by @DrEverr

I've also noticed the absence of the storage dictionary **s**. Is it located somewhere else, or should it actually be here?


## Comment by @tomusdrw

This order is incorrect. Note that order in the `codec.Class` object matters, since that's how we are going to serialize the object.

The structure so far was used for `info` host call serialization: https://graypaper.fluffylabs.dev/#/85129da/307902307902?v=0.6.3

However as you can see in the `TODO` above the struct it needs to be split to support state serialization as well:
https://graypaper.fluffylabs.dev/#/85129da/383303383b03?v=0.6.3


## Comment by @tomusdrw

```suggestion
 * Preimage dictionary entry.
```


## Comment by @tomusdrw

I feel like these comments are a bit redundant and IMHO the whole class/file is more readable without them, since they don't really show up anywhere and just take space. The comments for fields are more important and are basically duplicated. note that `codec.Class` enforces that all fields of the class need to have entry in this object.


## Comment by @tomusdrw

```suggestion
    /** `h`: Preimage hash. */
```

Current form is overly long.


## Comment by @tomusdrw

```suggestion
 * `δ[s]`: Service dictionary entry.
```

That's a pattern we're using in other places (i.e. `<symbol>: <description>`) 


## Comment by @tomusdrw

Wasn't needed so far. Feel free to add it here, however I'm going to rework that a bit as part of state serialization (#269). Most likely we will have separate dictionaries/state-entries for `serviceInfo`, `servicePreimages`, `serviceStorage`, `serviceHistoryLookup`, etc. Everything except for the first one will most likely need to be a multi-key dictionary, e.g. `(ServiceId, PreimageHash) -> PreimageBlob`, because we will be querying that on demand instead of querying everything at once (though that's yet to be decided),


## Comment by @tomusdrw

Added in #298 
