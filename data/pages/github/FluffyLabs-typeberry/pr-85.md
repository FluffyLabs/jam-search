---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/85'
title: 'PVM: move math operations to a separate file'
site: github.com/FluffyLabs/typeberry
created_at: '2024-08-16T06:25:43.000Z'
last_modified: '2024-08-16T06:25:43.000Z'
content_kind: pr
---

# PVM: move math operations to a separate file

## Pull Request by @mateuszsikora

# What?

- I moved part of math operations from `math-ops.ts` to `math-utils.ts`. At the beginning I want to move all of them but unfortunately signed division has to have access to registers (in different cases  the result can be signed or unsigned)
- I fixed add operation. The previous solution didn't work
- I got rid of BigInt as it is slow. It resolves one of the problems described here: https://github.com/FluffyLabs/typeberry/issues/13

# Why?
I want to reuse safe `add` in other places (i.e. indirect addressing)
 


## Comment by @tomusdrw

Can you add a few words of explanation of how this addition works? I have no doubt it's correct, I just can't wrap my head around that at the first glance.
Is it some known way of dealing with this kind of addition?


## Comment by @tomusdrw

```suggestion
/**
 * Overflowing subtraction for two-complement representation of 32-bit signed numbers.
 */ 
export function sub(a: number, b: number) {
```


## Comment by @tomusdrw

This seems pretty suspicious. Division is a more expensive operation than multiplication so I'm not sure if it's a good idea to divide every time we want to multiply.
Can you explain the rationale of this as well? Is the algorithm below some well-known algorithm to multiply the numbers? It looks a bit like Karatsuba to me, but I'm not sure.


## Comment by @tomusdrw

This can have now subtraction inlined instead of doing another call.


## Comment by @tomusdrw

What does the `upper` mean here? Can you add some documentation in what context this function should be used over `mulUnsigned`?


## Comment by @mateuszsikora

this is just my observation of a glass of water - instead of calculating how full it is (because it overflows), you can measure how empty it is. I don't know if there is any specific name for this method. GPT suggestion was: 
```ts
a + b >>> 0
```

`>>> 0` ensures that the result is 32 bit int. but I am not sure if there is no conversion to double under the hood 


## Comment by @mateuszsikora

so we want to calculate a * b, the result treats as 64 bit number and take upper 32 bits. PVM has a few special instructions to calculate it (UU, US and SS) 


## Comment by @mateuszsikora

we have to check somehow if `a*b > MAX_VALUE`, it was the simplest option. do you have a better idea ?


## Comment by @mateuszsikora

https://graypaper.fluffylabs.dev/#WyIxYjA4MWZlM2U3IiwiMjYiLG51bGwsbnVsbCxbIjxkaXYgY2xhc3M9XCJ0IG0wIHgxNjYgaDYgeTE0M2MgZmY3IGZzMCBmYzAgc2MwIGxzMCB3czBcIj4iLCI8ZGl2IGNsYXNzPVwidCBtMCB4MTY2IGg2IHkxNDNjIGZmNyBmczAgZmMwIHNjMCBsczAgd3MwXCI+Il1d


## Comment by @mateuszsikora

yup, the same in case of other operations except the division


## Comment by @mateuszsikora

added a comment


## Comment by @mateuszsikora

ok I replaced the division with `a >= 2 ** 16 || b >= 2 ** 16`. it doesn't cover all cases but should be good enough.


## Comment by @mateuszsikora

it looks similar to Karatsuba but it doesn't. I split 4 bytes number into two 2 bytes number and perform multiplication on it. the result of the multiplication can be longer than 4 bytes and this approach allows to handle the result as two 32 bit numbers. it this case I need only the lower part. the higher part you can see in `mulUpper` function 


## Comment by @tomusdrw

So a separate issue then?


## Comment by @tomusdrw

```suggestion
/**
 * Efficiently multiply the two given numbers modulo 2**32 (i.e. lower 32-bit part of the multiplication).
 * 
 * In case the numbers fit into 2**32 we simply calculate their multiplication.
 * In case the numbers are larger we split them into higher and lower bits
 * and perform the multiplication separately to make sure we don't overflow
 * the 2**32 and `MAX_SAFE_INTEGER`.
 */
export function mulLowerUnsigned(a: number, b: number) {
```

Also a good case for `type U32 = Opaque<number, a 32-byte number>` helper type that we could use `ensure` for from #86 :)


## Comment by @tomusdrw

The thresholds are correct here, but to optimize multiplication of numbers in range of `(2**16, 2**26 (sqrt(MAX_SAFE_INTEGER))` we could still use the regular JS multiplication since the result would fit `MAX_SAFE_INTEGER`.

However we would need to make sure that the result is always within 2**32 by setting the upper bits to 0.

I'd add it as a small possible gain for potential PVM improvements #13 to avoid the complications for now.


## Comment by @tomusdrw

```suggestion
/**
 * Multiply two unsigned 32-bit numbers and take the upper 32-bits of the result.
 *  
 * The result of multiplication is a 64-bits number and we are only interested in the part that lands in the upper 32-bits.
 * For example if we multiply `0xffffffff * 0xffffffff`, we get:
 
 * |   32-bits  |   32-bits  |
 * +------------+------------+
 * |    upper   |    lower   |
 * | 0xfffffffe | 0x00000001 |
 *
 * So `0xfffffffe` is returned.
 */
export function mulUpperUnsigned(a: number, b: number) {
```


## Comment by @tomusdrw

```suggestion
/**
 * Same as [mulUpperUnsigned] but treat the arguments as signed (two-complement) 32-bit numbers and the result alike.
 */
export function mulUpperSigned(a: number, b: number) {
```


## Comment by @mateuszsikora

no, I will change it in this PR. I had to go out so I pushed only part of fixes


## Comment by @mateuszsikora

yeah, lets merge #86 and I will add it here 


## Comment by @mateuszsikora

the optimal condition here is not easy to find without division :) I thought about some approximation of log2:
```ts
function log2(x: number) {
    let log = 0;
    if (x >= 1 << 16) { x >>= 16; log += 16; }
    if (x >= 1 << 8) { x >>= 8; log += 8; }
    if (x >= 1 << 4) { x >>= 4; log += 4; }
    if (x >= 1 << 2) { x >>= 2; log += 2; }
    if (x >= 1 << 1) { log += 1; }
    return log;
}
```
and then

```ts
if (log2(a) + log2(b) >= 32) {
  ...
} 
```
but number of "ifs" is not promising here.

Also I am wondering if the simples solution is not good enough, I mean:

```ts
if (a * b >= 2 ** 32) {
  // do safe multiplication
} else {
  return a * b
}
```
I don't know how expensive is conversion to double but this solution is not future-proof - it won't work when registers are 64-bit length


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

I checked a few solution and it looks that the division is not as bad so you think:

https://jsbenchmark.com/#eyJjYXNlcyI6W3siaWQiOiI1Q0xEUm80dkF5N1EwNXBBSWF1aEUiLCJjb2RlIjoibGV0IHQgPTA7XG5mb3IgKGxldCBpID0gMDsgaSA8IDEwMDAwMDA7IGkrKykge1xuICBpZiAoaSA--PSAxIDw8IDE2KSB7IHggPj49IDE2OyBsb2cgKz0gMTY7IH1cbiAgICBpZiAoeCA-PSAxIDw8IDgpIHsgeCA-Pj0gODsgbG9nICs9IDg7IH1cbiAgICBpZiAoeCA-PSAxIDw8IDQpIHsgeCA-Pj0gNDsgbG9nICs9IDQ7IH1cbiAgICBpZiAoeCA-PSAxIDw8IDIpIHsgeCA-Pj0gMjsgbG9nICs9IDI7IH1cbiAgICBpZiAoeCA-


## Comment by @mateuszsikora

added an item to #13 


## Comment by @tomusdrw

the `a * b > 2**32` would be good enough I think, but I'm actually afraid of this thing being worse for JIT optimisation (since we are no longer within 2**32 space and might turn into a double).

I think the current solution is good enough, and afaict see from the benchmarks also the fastest, so let's keep it and wait for proper benchmarking #2 to make any further decisions.
