---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/297'
title: '[278] host calls test vectors'
site: github.com/FluffyLabs/typeberry
created_at: '2025-03-20T22:26:08.000Z'
last_modified: '2025-03-20T22:26:08.000Z'
content_kind: pr
---

# [278] host calls test vectors

## Pull Request by @DrEverr

Finished importing Host Calls test vectors.

For testing, I recommend deleting the `template` folder. It contains JSON files that don’t match the Host Calls objects they’re supposed to represent. The template JSON files throw parsing errors and aren’t useful at all.
All Host Calls are successfully parsed and ready for testing. 

Additionally:
- Moved `uint8Array` and `bigUint64Array` from `pvm` to `common` and refactored the error codes they throw.
- Added `bigUint64` as a representative of bigint numbers.
- Added `record` to parse dictionary-like objects.


## Comment by @DrEverr

waiting for a `FluffyLabs/jamtestvectors` Host Call Vector merge


## Comment by @DrEverr

I'm not sure about this. It should parses "" when field is of type H.


## Comment by @tomusdrw

```suggestion
      v.length === 0 ? Bytes.zero(32).asOpaque() : Bytes.parseBytes(v, 32).asOpaque(),
```


## Comment by @tomusdrw

Is it required for the test vectors? I think it would be better to be strict here and require exactly 0x-prefixed 32 bytes, we could add a separate method to parse the potentially empty string.
