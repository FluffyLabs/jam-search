---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/97'
title: Initial version of the Logger
site: github.com/FluffyLabs/typeberry
created_at: '2024-09-05T21:55:25.000Z'
last_modified: '2024-09-05T21:55:25.000Z'
content_kind: pr
---

# Initial version of the Logger

## Pull Request by @tomusdrw

Follow up on #91 

A very first version of the logger. We can iterate on it :)

Some ideas that I have:
1. Spawn a separate worker thread that is only responsible for logging to console/file - the workers need to synchronize access to `console` anyway, so it actually makes sense to make it explicit. In such case the logging to console would actually be `postMessage` on some channel. We need to compare the performance though.
2. Format the output with colors (`Colored` transport)
3. Redirect the output to a file (rotating, etc; `File` transport).
4. Improve how define the level of a module - currently we need to parse the module name and extract parent modules. Instead of having a global config, we could registers the loggers in some global array and each one of them could have a separate configuration. That should be faster (measure!) but we would need to make sure we never leak loggers (i.e. create some loggers dynamically and drop them).


## Comment by @mateuszsikora

I liked the simplicity of the previous version :(


## Comment by @mateuszsikora

shouldn't the constructor be private in this case?


## Comment by @mateuszsikora

if `debug` and `log` are the same - why do we need both?


## Comment by @mateuszsikora

```suggestion
    if (clean.includes("=")) {
      const [mod, lvl] = clean.split("=");
      modules.set(mod.trim(), parseLevel(lvl.trim()));
    } else {
      defLevel = parseLevel(clean);
    }
```


## Comment by @mateuszsikora

I guess this "OK" will be visible in logs, right? don't you think it can be difficult to figure out what does it mean without looking at the test runnner code ?
```suggestion
  return "Tests have been executed";
```


## Comment by @mateuszsikora

don't you think it would be nice to add some timestamp (especially if you want to use a separate worker to collect logs)?


## Comment by @mateuszsikora

```suggestion
  push(level: Level, moduleName: string, fileName: string, val: string) {
    const shortName = fileName.replace(this.options.workingDir, "");
    const configuredLevel = findLevel(this.options, moduleName);
    const lvlText = Level[lvl];
    if (level < configuredLevel) {
      return;
    }
```


## Comment by @tomusdrw

The whole logger is designed to mimic `RUST_LOG` a bit. I think people are familiar with it, hence the alternative naming for `debug`-logging.


## Comment by @tomusdrw

Cool! Didn't know it's actually possible :)


## Comment by @tomusdrw

💔


## Comment by @tomusdrw

Yes, but I'd rather do that in a separate PR, since it will require some benchmarks. Just taking `Date.now` might be way too expensive (Getting a timestamp is actually a system call, not only we get from JS -> VM but the VM needs to get the time for the OS). So I'd rather start with something simple to not hinder the performance without proper measurements.


## Comment by @mateuszsikora

I see but possible values in Rust are:
```
error
warn
info
debug
trace
off
```
so why do we have additional `log` option ?


## Comment by @tomusdrw

Because that's what JS uses. It's the same as the `debug` level.
Hence, when configuring from the ENV variable we can put both `debug` and `log`. From the code side  though we use more JS-idiomatic `log` (i.e. it will be easier to replace `console.log` with `logger.log` than to learn that it should now be `logger.debug` :))


## Comment by @mateuszsikora

yeah I understand but it doesn't make any sense to keep both values in config. I would suggest to keep one (debug) and we can have two methods `logger.debug(...)` and `logger.log(...)` (log can be an alias to debug or the other way around). In this case the config will be simpler. 

also there is one more problem - I think `LOG_LEVEL=LOG` means nothing if you don't know that `LOG` is the same what `DEBUG`, doesn't it?


## Comment by @tomusdrw

Sorry, I don't get it. I don't want an alias in the code: there should be just `logger.log`, since LOG is more idiomatic for JS.

`DEBUG` alias is only available in the EVN variable during configuration and it's an alias for `LOG` for a convenience for people more familiar with RUST_LOG env. If you believe it's too complicated let's remove that alias and just use `LOG` consistently everywhere, I don't want to introduce even more aliases.


## Comment by @mateuszsikora

ok makes sense. I don't know why but I was sure that you have `debug` and `log` values of ENV var and also two methods. never mind.
