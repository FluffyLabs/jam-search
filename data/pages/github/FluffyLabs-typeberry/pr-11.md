---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/11'
title: 'PVM: partial ops support'
site: github.com/FluffyLabs/typeberry
created_at: '2024-07-12T22:34:05.000Z'
last_modified: '2024-07-12T22:34:05.000Z'
content_kind: pr
---

# PVM: partial ops support

## Pull Request by @mateuszsikora

I added support for part of instructions (math and bit operations):

<img width="177" alt="Screenshot 2024-07-15 at 23 18 08" src="https://github.com/user-attachments/assets/dfb5c96f-2005-4dac-b64d-a7aa1d29ba90">




## Comment by @tomusdrw

Looks awesome! I left a bunch of grumbles - I know it's still Work in Progress, but I don't want to re-review the entire PVM code again, it's easier for me to bring up potential issues asap, hence that review might look like it's maybe "too deep".
Feel free to log some of that stuff into separate issues that can be fixed asynchronously.


## Comment by @tomusdrw

What does the `0->1` change signify here? AFAICT the last element of the tuple is Gas, why does `TRAP` require 1 gas?


## Comment by @tomusdrw

Can we drop `ArgsOf` everywhere? I feel that `instructionsWithOneImmediate` convey the same and is shorter to read.


## Comment by @tomusdrw

Here I'd drop the entire `ArgumentsOf` as well. If `withTwoRegisters` does not sound good, we might as well switch to `instructionsUsingTwoRegistersAndTwoImmedaties` (alternatively `Requiring` or `Needing`)


## Comment by @tomusdrw

```suggestion
const HIGHEST_INSTRUCTION_NUMBER = 87;
```

Might be better in `instruction` file? Also I'd consider adding some assert for that, to make sure it fails if we ever add more instructions than this. (unless simply assigning to the `Uint8Array` fails in case we go oob).


## Comment by @tomusdrw

Just checked that assigning to an index higher than length just silently fails, which is not the best behaviour.

Honestly I'd do something like:
```
for (const i of instruction) {
  assert(instructionArgumentTypeMap[i] !== null);
}
```



## Comment by @tomusdrw

Why have separate arrays for that instead of having the definition somewhere near the instruction itself?


## Comment by @tomusdrw

Not a fan of the filename :) Can you consider renaming to `instructionArgsAndGas.ts` or `instructionsMetadata.ts`? I don't see anything else here.


## Comment by @tomusdrw

Can we use the key name (lowercase) instead of having a bunch of strings in the assemblify? Seems that we could build a reverse lookup (opcode -> name) here?


## Comment by @tomusdrw

Maybe just rename the `numer-utils.ts` (additionally note a typo here) to `decode-immediate.ts`? Why generalise the file name if there is nothing more here? :)


## Comment by @tomusdrw

Can access `-1` in case `n == 0`. Consider putting an assert in the beginning of the function to throw an error in such case.


## Comment by @tomusdrw

nitpick: It could have a bit less operations if:
1. You started from the end
2. You just byte-shift `value` by 8

Roughly, if I'm not mistaken
```js
let value = bytes[n - 1];
for (let i = n - 2; i>=0; i--) {
  value <<= 8;
  value |= bytes[i];
}
```


## Comment by @tomusdrw

```suggestion
		for (let i = n; i < 4; i++) {
```


## Comment by @tomusdrw

I think if we've reversed everything the function is doing the code could have been a bit simpler (and also faster), i.e:

1. Check the sign bit and go into either positive or negative mode.
2. Set the initial `value` accordingly (either to `0` or `~0`).
3. Start a for loop where:
4. you shift the `value` by `8`
5. and then `|=` bytes in case of positive mode and `&=` bytes in case of negative mode.

Since this function is hot, I'd be really into making sure it uses as little ops as possible (obv we should micro-benchmark too). Might be left as a separate issue though.


## Comment by @tomusdrw

magic constant `13` - please name it.


## Comment by @tomusdrw

`24` magical as well. BTW shouldn't it be `counter + 24`?)


## Comment by @tomusdrw

This function is always returning `0`? If it's not finished yet, please add a TODO, cause now it's pretending to be working, while I think it isn't.


## Comment by @tomusdrw

Note: that's most likely going to be too slow (too many ops), we might need to keep track of `byteNumber/mask` alongside the `pc` (complexity) or pre-compute counter->isInstruction (risky).
Alternatively we can also try to have a semi-lazy cache for this (i.e. always fill up the cache using the full byte we read from `k`).

No need to fix it now, but please make an issue for this - I'd suggest a meta issue, where we're going to add many possible improvements that would later be evaluated with benchmarks.


## Comment by @tomusdrw

Since it's only for `decoding` an easy fix here might be to have a top-level 4-element array, that you fill up and re-use while passing to `decodeImmediate`.


## Comment by @tomusdrw

Is this behavior defined in gray paper? Do we simply ignore bytes that are not instructions? I think processing the instruction should rather move the `pc` accordingly to how many bytes it read.


## Comment by @tomusdrw

Should happen before and should fail if there is not enough :)


## Comment by @tomusdrw

As mentioned earlier, the `getArgs` function might need to return number of bytes it read to move the pc accordingly.


## Comment by @tomusdrw

same constant as the magic `13` earlier.


## Comment by @tomusdrw

I think it's misleading to have both `signedRegisters` and `unsignedRegisters` - it kind of implies that there are different sets of registers, while we only have different views.

WDYT about renaming these to `asSigned/asI64` and `asUnsigned/asU64`, so at the call site, it would be:
```
this.registers.asSigned[i]
```


## Comment by @tomusdrw

AFAIR `index` should be clamped in`0..12`


## Comment by @tomusdrw

`index` should be clamped between `0..12` or I'd suggest an assertion here if that constraint is enforced elsewhere


## Comment by @tomusdrw

BTW - perhaps let's introduce another type for register index - then any assertions/logic regarding clamping would be defined in a place where we cast the number to `RegisterIndex` I think it would also help readabilty too.


## Comment by @tomusdrw

I also re-read the code again, and IMHO the API for the `Registers` is not restrictive enough:
1. You can `.set` and `.get` on RAW registers (`this.registers.set`)
2. You can also set and get on any views, but using `[]` operator which is hard to track.

While the latter is probably okay for performance reasons, I think I'd remove the `set/get` methods (which I believe are only used for initial assignment - just use `asU64/asUnsigned`) and made sure that we clamp the register index correctly at the call site.


## Comment by @mateuszsikora

https://github.com/koute/jamtestvectors/blob/master_pvm_initial/pvm/programs/inst_trap.json

it looks `TRAP` is expensive :P 


## Comment by @mateuszsikora

I wanted to keep naming from GP but you are right, I will change it 


## Comment by @mateuszsikora

it is copy-paste from GP. I will change it


## Comment by @mateuszsikora

we discussed that objects/maps are expensive and we want to use arrays instead 


## Comment by @mateuszsikora

getArgs function will be replaced soon 


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

this file is removed


## Comment by @mateuszsikora

`decodeImmediate` function is replaced with `ImmediateDecoder` class because immediate as well as registers I have to have possibility to read as signed/unsigned values 


## Comment by @mateuszsikora

I added a condition to handle it 


## Comment by @mateuszsikora

this logic is not riding with us anymore


## Comment by @mateuszsikora

sorry bro, I removed it 🤷 


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

yup it should be. it worked because test programs aren't longer than 24 bytes. also there should be `<=` instead of `<` as length of args can be 24 


## Comment by @mateuszsikora

exported 24 to const and fixed the condition 


## Comment by @mateuszsikora

fixed


## Comment by @mateuszsikora

yeah there was something like `if (...) break; else break;` and it was always 0 but I fixed it sometime ago and now it should work correctly 


## Comment by @mateuszsikora

https://github.com/FluffyLabs/typeberry/issues/13


## Comment by @mateuszsikora

it is only decoding but in the future there will be more places like that so I am going to wrap program code into class and keep the code as array buffer to have possibility to use data view on it. But I will do it in a separate PR


## Comment by @mateuszsikora

yup there is skip function that returns no of bytes to skip. (number of `0` between instructions in bit mask). it works exactly the same. in the current code it is changed - `ArgsDecoder` returns number of bytes that were consumed 


## Comment by @mateuszsikora

yeah I know it. it was added because pvm test runner checks gas after program execution. I will change that in a separate PR


## Comment by @mateuszsikora

done  - `ArgsDecoder` returns number of bytes to skip 


## Comment by @mateuszsikora

fixed


## Comment by @mateuszsikora

this case is specified: 

<img width="1030" alt="Screenshot 2024-07-15 at 23 04 51" src="https://github.com/user-attachments/assets/9ce161c0-4f14-4552-97fb-68e878573b9e">

and it will be handled in a separate PR


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

I think we don't need this reverse lookup. I am going to remove asemblify logic soon 


## Comment by @mateuszsikora

outdated


## Comment by @mateuszsikora

renamed


## Comment by @mateuszsikora

some typescript love is needed here but I didn't want to waste time on that now because I am not sure if it is the final version 


## Comment by @mateuszsikora

I am not sure if it is not a premature optimisation


## Comment by @tomusdrw

```suggestion
export type NoArgumentsResult = {
   argumentType: ArgumentType.NO_ARGUMENTS;
   noOfInstructionsToSkip: number;
};
export type ThreeRegistersResult = {
  argumentType: ArgumentType.THREE_REGISTERS;
  noOfInstructionsToSkip: number;
  firstRegisterIndex: number;
  secondRegisterIndex: number;
  thirdRegisterIndex: number;
};
export type TwoRegistersOneImmediateResult = {
  argumentType: ArgumentType.TWO_REGISTERS_ONE_IMMEDIATE;
  noOfInstructionsToSkip: number;
  firstRegisterIndex: number;
  secondRegisterIndex: number;
  immediateDecoder1: ImmediateDecoder;
};
```

I honestly feel that just listing the fields is much simpler than the type magic :)

TypeScript will figure out the common part anyway - and thanks to this approach  you clearly see everything that's there.


## Comment by @tomusdrw

IMHO we should be using `null's` instead of `undefined` (pretty much everywhere when we expect things to be not set)
Might be worth adding `Nullable` type helper if it's not part of std lib.


## Comment by @tomusdrw

Might be - will need benchmarking. Another option to try is use an `ArrayBuffer`. Accessing objects might involve some hashing (slow) and there is no guarantee that they are nearby in terms of physical memory.


## Comment by @tomusdrw

Why not do that in `setByte` and split into two bytes (i.e. make `setByte` more expensive, but then just return first/second index immediately).


## Comment by @tomusdrw

I still think it's worth to add a check that we actually set something for all instructions (maybe just a test that reads from `instructionArgumentTypeMap` and asserts that the result is not undefined)?


## Comment by @tomusdrw

Seems like it's duplicated in `instruction-argument-type-map`?


## Comment by @tomusdrw

might be worth extracting `32` as `MAX_SHIFT` or sth


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

I added a test to check if all instructions are mapped


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

fixed


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

because sometimes I expect 2 registers encoded on a single byte and sometimes only one register so the current approach is "cheaper" in case of only 1 register


## Comment by @mateuszsikora

done


## Comment by @mateuszsikora

done


## Comment by @tomusdrw

Only in case you are not calling `getFirstIndex` many times ;)
