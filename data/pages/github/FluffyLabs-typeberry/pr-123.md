---
type: page
url: 'https://github.com/FluffyLabs/typeberry/pull/123'
title: Codec improvements
site: github.com/FluffyLabs/typeberry
created_at: '2024-09-24T19:49:56.000Z'
last_modified: '2024-09-24T19:49:56.000Z'
content_kind: pr
---

# Codec improvements

## Pull Request by @tomusdrw

1. Method to quickly decode a single object from some bytes source.
2. Method to quickly encode a single object, taking `sizeHintBytes` into consideration.
3. Nested views.
4. Move `materialize` method to a symbol to avoid potential name clashes.


## Comment by @mateuszsikora

```suggestion
      const key = descriptorKeys[i] as keyof DescriptorRecord<T>;
```


## Comment by @mateuszsikora

```suggestion
      const descriptor = this.descriptors[k];
```


## Comment by @mateuszsikora

why is it called`maybeView`? I can't see any possibility that it is `undefined` / `null` 


## Comment by @mateuszsikora

Are you sure we need it ? `materialize` is a verb so there is low probability that someone names a field using this word. If you really want to avoid clashes you should use symbols for each `View` member 


## Comment by @mateuszsikora

can you postpone this change till it is a problem?


## Comment by @mateuszsikora

in `Decoder` you use `Decoder.object` that basically do the same. I assume you don't use it here because of `expectedLength` option but wouldn't be the same (but simpler) to do something like:

```ts
const encoder = Encoder.create();
Encoder.object(encode, object);
return encoder.viewResult();
```


## Comment by @tomusdrw

Ah: artifact from previous approach. Yes, it's always a `view` not `maybeView` :)



## Comment by @tomusdrw

If you think it's not needed, then sure. I think it makes a clear distinction between the things that are coming from the class itself and the view-specific method.

> If you really want to avoid clashes you should use symbols for each View member

Yeah, fair point. There can be a name clash if you have two fields called: `something` and `somethingView` directly on the class itself, but to me that seems even less likely than the `materialize` method.

I can revert back to `materialize` if you prefer - no strong feelings about this - let me know.


## Comment by @tomusdrw

Sure!


## Comment by @mateuszsikora

ok I see your point:
<img width="874" alt="Screenshot 2024-09-25 at 11 31 42" src="https://github.com/user-attachments/assets/48f94823-109f-4c89-a413-971d4e611b0a">
let's leave it like it is. I have no better idea how to distinguish "data" members" from "utility" members


## Comment by @tomusdrw

We can use it here, but I'd keep the `expectedLength` though. Without it we will over-allocate for small objects. I've decided to inline the method to avoid having these extra function calls and the `applySizeHint` which won't do anything, but perhaps since you're asking it's better to have it more explicit.


## Comment by @tomusdrw

Already reverted:D we can always introduce that if it's annoying.
